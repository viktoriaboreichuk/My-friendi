name: Build Lunari APK (legacy 0.2.5)

# Legacy workflow retained only for historical/manual diagnostics.
# Do not run automatically on PRs: it reconstructs the old 0.2.5 background
# from Base64 fragments and is intentionally superseded by build-026-from-025.yml.
on:
  workflow_dispatch:

jobs:
  legacy-info:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "Legacy Lunari 0.2.5 workflow."
          echo "Use Build Lunari 0.2.6 from 0.2.5 for current development."
