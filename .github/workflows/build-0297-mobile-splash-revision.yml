name: Build Lunari 0.2.9.7 Mobile portrait splash revision (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow only
        run: echo "0.2.9.7 splash workflow is retired; use the current splash workflow."
