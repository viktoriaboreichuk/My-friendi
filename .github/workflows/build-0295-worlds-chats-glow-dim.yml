name: Build Lunari 0.2.9.5 Worlds/Chats glow dim (legacy)

# Historical workflow retained only for manual diagnostics.
# Automatic PR execution is disabled: current patches have their own scoped workflows.
on:
  workflow_dispatch:

jobs:
  legacy-info:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "Legacy Lunari 0.2.9.5 workflow."
          echo "Use the workflow dedicated to the current patch instead."
